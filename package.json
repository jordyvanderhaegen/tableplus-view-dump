{
  "name": "tableplus-view-dump",
  "type": "module",
  "description": "Copy create or drop syntax for database views",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "devDependencies": {
    "vite": "^5.4.10",
    "vite-plugin-static-copy": "^2.0.0"
  }
}
